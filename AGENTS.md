# AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live in this repo's GitHub Issues (`ravencloak-org/RavenScale`), via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
