# RavenScale

> Scaffolding placeholder. Real project description is pending the source document
> that was meant to accompany the kickoff (it was referenced as "the attached
> document" but no attachment was present). Once that lands, this README and the
> `grill-with-docs` session will be populated with actual scope, terminology, and
> decisions.

## Status

- [x] Repo initialized
- [ ] Source document received
- [ ] Scope / domain model captured
- [ ] `grill-with-docs` session run in the `#ravenscale` channel
